import math
a,b=input().split()
i=0
n=0
count=0
x=int(a)
y=int(b)
while i<=x-1:
    z=input().split()

    while n <= len(z) - 1:
        road = float(int(z[n]) / int(y))
        count += math.ceil(road)
        n += 1
    print(count)
    i += 1
    break
